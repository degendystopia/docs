## Degen Dystopia Frontend

Developed using next.js / react.

## Install dependencies

Before running any command, make sure to install dependencies:

```sh
$ npm install
```

## Export to static

This command should export the static files in to /out folder:

```sh
$ npm run export
```
